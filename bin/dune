(executable
 (public_name hmm_sampler)
 (name main)
 (libraries containers))

(env
 (_
  (flags
   :standard
   -w
   -32
   -warn-error
   -a+8
   -open
   Containers
   -strict-sequence
   -safe-string)))
